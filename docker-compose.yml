services:
  quiz_calendar:
    build: .
    restart: always
    ports:
      - "6000:6000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/calendar_bot.sqlite3
